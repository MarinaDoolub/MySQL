CREATE TABLE Books(
				   book_id int,
                   title varchar(50),
                   author_id int,
                   genre varchar(25),
                   published_year int,
                   available bool
);