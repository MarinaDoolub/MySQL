-- This query shows all members from the Members table
-- It sorts the results by last name, from Z to A (descending order)
-- The ORDER BY keyword is used to sort
-- DESC means descending (reverse) order
SELECT * FROM Members
ORDER BY last_name DESC;